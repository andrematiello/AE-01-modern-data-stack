version: 2

sources:
  - name: raw
    description: "Raw market data loaded by dlt from Yahoo Finance (yfinance)."
    schema: raw
    tables:
      - name: prices
        description: "Daily OHLCV per ticker — one row per ticker per trading day."
      - name: tickers
        description: "Descriptive metadata per ticker (name, sector, industry)."
